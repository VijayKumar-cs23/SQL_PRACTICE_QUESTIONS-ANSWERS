SQL PRACTICE QUESTIONS :

1.WAQTD the name of all the employees from the EMP table.

SELECT ENAME
FROM EMP;

2.WAQTD the salary of all the employees from the EMP table.

SELECT SAL
FROM EMP;

3.WAQTD the department number of all the employees.

SELECT DEPTNO
FROM EMP;

4.WAQTD the name and salary of the employees from the EMP table.

SELECT ENAME, SAL
FROM EMP;

5.WAQTD the name, job and department number of all the employees.

SELECT ENAME, JOB, DEPTNO
FROM EMP;

6.WAQTD the employee number, name, job and salary of all the employees.

SELECT EMPNO, ENAME, JOB, SAL
FROM EMP;

7.WAQTD the name, salary, commission and department number of all the employees.

SELECT ENAME, SAL, COMM, DEPTNO
FROM EMP;

8.WAQTD all the details of the employees from the EMP table.

SELECT *
FROM EMP;

9.WAQTD the name and annual salary of all the employees.

SELECT ENAME, SAL * 12
FROM EMP;

10.WAQTD the name and salary after adding 500 to the salary of all employees.

SELECT ENAME, SAL + 500
FROM EMP;

11.WAQTD the name and salary after increasing the salary by 10%.

SELECT ENAME, SAL + (SAL * 10 / 100)
FROM EMP;

12.WAQTD the name and total annual salary after adding a yearly bonus of 2000.

SELECT ENAME, (SAL * 12) + 2000
FROM EMP;

13.WAQTD the name and annual salary of all employees with a suitable alias for annual salary.

SELECT ENAME, SAL * 12 AS ANNUAL_SALARY
FROM EMP;

14.WAQTD the employee number as ID, employee name as NAME and salary as MONTHLY_SALARY.

SELECT EMPNO AS ID,ENAME AS NAME,SAL AS MONTHLY_SALARY
FROM EMP;

15.WAQTD all the different jobs available in the EMP table.

SELECT DISTINCT JOB
FROM EMP;

16.WAQTD all the different department numbers from the EMP table.

SELECT DISTINCT DEPTNO
FROM EMP;

17.WAQTD all the different salaries available in the EMP table.

SELECT DISTINCT SAL
FROM EMP;

18.WAQTD the employee name, monthly salary and annual salary.

SELECT ENAME, SAL, SAL * 12 AS ANNUAL_SALARY
FROM EMP;

19.WAQTD the employee name, salary and salary after a 20% increment.

SELECT ENAME,SAL,SAL+(SAL*20/100) AS INCREASED_SALARY
FROM EMP;

SELECTION :

20. WAQTD the details of the employees who are working in department 10.

SELECT * FROM EMP
WHERE DEPTNO = 10;

21. WAQTD the name and job of employees whose job is MANAGER.

SELECT ENAME, JOB FROM EMP
WHERE JOB = 'MANAGER';

22. WAQTD the name and salary of employees whose salary is greater than 3000.

SELECT ENAME, SAL FROM EMP
WHERE SAL > 3000;

23. WAQTD the name and salary of employees whose salary is less than 1000.

SELECT ENAME, SAL FROM EMP
WHERE SAL < 1000;

24. WAQTD the details of employees whose salary is greater than or equal to 3000.

SELECT * FROM EMP
WHERE SAL >= 3000;

25. WAQTD the details of employees whose salary is less than or equal to 1250.

SELECT * FROM EMP
WHERE SAL <= 1250;

26. WAQTD the details of employees who are not working in department 10.

SELECT * FROM EMP
WHERE DEPTNO <> 10;

27. WAQTD the details of employees who are working in department 20 and earning more than 2000.

SELECT * FROM EMP
WHERE DEPTNO = 20 AND SAL > 2000;

28. WAQTD the name and salary of employees who are working in department 30 and earning more than 1500.

SELECT ENAME, SAL FROM EMP
WHERE DEPTNO = 30 AND SAL > 1500;

29. WAQTD the details of employees whose job is MANAGER and salary is greater than 2500.

SELECT * FROM EMP
WHERE JOB = 'MANAGER' AND SAL > 2500;

30. WAQTD the name and salary of employees whose salary is greater than 2000 and less than 4000.

SELECT ENAME, SAL FROM EMP
WHERE SAL > 2000 AND SAL < 4000;

31. WAQTD the details of employees who are working in department 10 or department 20.

SELECT * FROM EMP
WHERE DEPTNO = 10 OR DEPTNO = 20;

32. WAQTD the details of employees whose job is CLERK or MANAGER.

SELECT * FROM EMP
WHERE JOB = 'CLERK' OR JOB = 'MANAGER';

33. WAQTD the name and salary of employees whose salary is 1250 or 3000.

SELECT ENAME, SAL FROM EMP
WHERE SAL = 1250 OR SAL = 3000;

34. WAQTD the details of employees who are not working in department 30.

SELECT * FROM EMP
WHERE NOT DEPTNO = 30;

35. WAQTD the details of employees whose job is not SALESMAN.

SELECT * FROM EMP
WHERE NOT JOB = 'SALESMAN';

36. WAQTD the name and salary of employees whose salary is not 3000.

SELECT ENAME, SAL FROM EMP
WHERE NOT SAL = 3000;

37. WAQTD the details of employees who are working in department 10, 20 or 30.

SELECT * FROM EMP
WHERE DEPTNO IN (10, 20, 30);

38. WAQTD the details of employees whose job is CLERK, MANAGER or ANALYST.

SELECT * FROM EMP
WHERE JOB IN ('CLERK', 'MANAGER', 'ANALYST');

39. WAQTD the name and salary of employees whose salary is 800, 1250 or 3000.

SELECT ENAME, SAL FROM EMP
WHERE SAL IN (800, 1250, 3000);

40. WAQTD the details of employees who are working in department 10 or 30 using IN.

SELECT * FROM EMP
WHERE DEPTNO IN (10, 30);








