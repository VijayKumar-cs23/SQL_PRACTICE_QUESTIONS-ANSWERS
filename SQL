SQL PRACTICE QUESTIONS :

1.WAQTD the name of all the employees from the EMP table.

SELECT ENAME
FROM EMP;

2.WAQTD the salary of all the employees from the EMP table.

SELECT SAL
FROM EMP;

3.WAQTD the department number of all the employees.

SELECT DEPTNO
FROM EMP;

4.WAQTD the name and salary of the employees from the EMP table.

SELECT ENAME, SAL
FROM EMP;

5.WAQTD the name, job and department number of all the employees.

SELECT ENAME, JOB, DEPTNO
FROM EMP;

6.WAQTD the employee number, name, job and salary of all the employees.

SELECT EMPNO, ENAME, JOB, SAL
FROM EMP;

7.WAQTD the name, salary, commission and department number of all the employees.

SELECT ENAME, SAL, COMM, DEPTNO
FROM EMP;

8.WAQTD all the details of the employees from the EMP table.

SELECT *
FROM EMP;

9.WAQTD the name and annual salary of all the employees.

SELECT ENAME, SAL * 12
FROM EMP;

10.WAQTD the name and salary after adding 500 to the salary of all employees.

SELECT ENAME, SAL + 500
FROM EMP;

11.WAQTD the name and salary after increasing the salary by 10%.

SELECT ENAME, SAL + (SAL * 10 / 100)
FROM EMP;

12.WAQTD the name and total annual salary after adding a yearly bonus of 2000.

SELECT ENAME, (SAL * 12) + 2000
FROM EMP;

13.WAQTD the name and annual salary of all employees with a suitable alias for annual salary.

SELECT ENAME, SAL * 12 AS ANNUAL_SALARY
FROM EMP;

14.WAQTD the employee number as ID, employee name as NAME and salary as MONTHLY_SALARY.

SELECT EMPNO AS ID,ENAME AS NAME,SAL AS MONTHLY_SALARY
FROM EMP;

15.WAQTD all the different jobs available in the EMP table.

SELECT DISTINCT JOB
FROM EMP;

16.WAQTD all the different department numbers from the EMP table.

SELECT DISTINCT DEPTNO
FROM EMP;

17.WAQTD all the different salaries available in the EMP table.

SELECT DISTINCT SAL
FROM EMP;

18.WAQTD the employee name, monthly salary and annual salary.

SELECT ENAME, SAL, SAL * 12 AS ANNUAL_SALARY
FROM EMP;

19.WAQTD the employee name, salary and salary after a 20% increment.

SELECT ENAME,SAL,SAL+(SAL*20/100) AS INCREASED_SALARY
FROM EMP;

SELECTION :

20. WAQTD the details of the employees who are working in department 10.

SELECT * FROM EMP
WHERE DEPTNO = 10;

21. WAQTD the name and job of employees whose job is MANAGER.

SELECT ENAME, JOB FROM EMP
WHERE JOB = 'MANAGER';

22. WAQTD the name and salary of employees whose salary is greater than 3000.

SELECT ENAME, SAL FROM EMP
WHERE SAL > 3000;

23. WAQTD the name and salary of employees whose salary is less than 1000.

SELECT ENAME, SAL FROM EMP
WHERE SAL < 1000;

24. WAQTD the details of employees whose salary is greater than or equal to 3000.

SELECT * FROM EMP
WHERE SAL >= 3000;

25. WAQTD the details of employees whose salary is less than or equal to 1250.

SELECT * FROM EMP
WHERE SAL <= 1250;

26. WAQTD the details of employees who are not working in department 10.

SELECT * FROM EMP
WHERE DEPTNO <> 10;

27. WAQTD the details of employees who are working in department 20 and earning more than 2000.

SELECT * FROM EMP
WHERE DEPTNO = 20 AND SAL > 2000;

28. WAQTD the name and salary of employees who are working in department 30 and earning more than 1500.

SELECT ENAME, SAL FROM EMP
WHERE DEPTNO = 30 AND SAL > 1500;

29. WAQTD the details of employees whose job is MANAGER and salary is greater than 2500.

SELECT * FROM EMP
WHERE JOB = 'MANAGER' AND SAL > 2500;

30. WAQTD the name and salary of employees whose salary is greater than 2000 and less than 4000.

SELECT ENAME, SAL FROM EMP
WHERE SAL > 2000 AND SAL < 4000;

31. WAQTD the details of employees who are working in department 10 or department 20.

SELECT * FROM EMP
WHERE DEPTNO = 10 OR DEPTNO = 20;

32. WAQTD the details of employees whose job is CLERK or MANAGER.

SELECT * FROM EMP
WHERE JOB = 'CLERK' OR JOB = 'MANAGER';

33. WAQTD the name and salary of employees whose salary is 1250 or 3000.

SELECT ENAME, SAL FROM EMP
WHERE SAL = 1250 OR SAL = 3000;

34. WAQTD the details of employees who are not working in department 30.

SELECT * FROM EMP
WHERE NOT DEPTNO = 30;

35. WAQTD the details of employees whose job is not SALESMAN.

SELECT * FROM EMP
WHERE NOT JOB = 'SALESMAN';

36. WAQTD the name and salary of employees whose salary is not 3000.

SELECT ENAME, SAL FROM EMP
WHERE NOT SAL = 3000;

37. WAQTD the details of employees who are working in department 10, 20 or 30.

SELECT * FROM EMP
WHERE DEPTNO IN (10, 20, 30);

38. WAQTD the details of employees whose job is CLERK, MANAGER or ANALYST.

SELECT * FROM EMP
WHERE JOB IN ('CLERK', 'MANAGER', 'ANALYST');

39. WAQTD the name and salary of employees whose salary is 800, 1250 or 3000.

SELECT ENAME, SAL FROM EMP
WHERE SAL IN (800, 1250, 3000);

40. WAQTD the details of employees who are working in department 10 or 30 using IN.

SELECT * FROM EMP
WHERE DEPTNO IN (10, 30);

41. WAQTD the details of employees who are not working in department 10 or 20.

SELECT * FROM EMP
WHERE DEPTNO NOT IN (10, 20);

42. WAQTD the details of employees whose job is neither CLERK nor MANAGER.

SELECT * FROM EMP
WHERE JOB NOT IN ('CLERK', 'MANAGER');

43. WAQTD the details of employees whose salary is between 1000 and 3000.

SELECT * FROM EMP
WHERE SAL BETWEEN 1000 AND 3000;

44. WAQTD the name and salary of employees whose salary is between 2000 and 4000.

SELECT ENAME, SAL FROM EMP
WHERE SAL BETWEEN 2000 AND 4000;

45. WAQTD the details of employees whose employee number is between 7500 and 7800.

SELECT * FROM EMP
WHERE EMPNO BETWEEN 7500 AND 7800;

46. WAQTD the details of employees who were hired between 01-JAN-1981 and 31-DEC-1981.

SELECT * FROM EMP
WHERE HIREDATE BETWEEN '01-JAN-1981' AND '31-DEC-1981';

47. WAQTD the details of employees whose salary is not between 1000 and 3000.

SELECT * FROM EMP
WHERE SAL NOT BETWEEN 1000 AND 3000;

48. WAQTD the name and salary of employees whose salary is not between 1500 and 3000.

SELECT ENAME, SAL FROM EMP
WHERE SAL NOT BETWEEN 1500 AND 3000;

49. WAQTD the details of employees whose name starts with S.

SELECT * FROM EMP
WHERE ENAME LIKE 'S%';

50. WAQTD the details of employees whose name starts with A.

SELECT * FROM EMP
WHERE ENAME LIKE 'A%';

51. WAQTD the details of employees whose name ends with S.

SELECT * FROM EMP
WHERE ENAME LIKE '%S';

52. WAQTD the details of employees whose name ends with N.

SELECT * FROM EMP
WHERE ENAME LIKE '%N';

53. WAQTD the details of employees whose name contains the letter A.

SELECT * FROM EMP
WHERE ENAME LIKE '%A%';

54. WAQTD the details of employees whose name contains the letter M.

SELECT * FROM EMP
WHERE ENAME LIKE '%M%';

55. WAQTD the details of employees whose name has exactly four characters.

SELECT * FROM EMP
WHERE ENAME LIKE '____';

56. WAQTD the details of employees whose name has exactly five characters.

SELECT * FROM EMP
WHERE ENAME LIKE '_____';

57. WAQTD the details of employees whose name starts with S and has exactly five characters.

SELECT * FROM EMP
WHERE ENAME LIKE 'S____';

58. WAQTD the details of employees whose second character is A.

SELECT * FROM EMP
WHERE ENAME LIKE '_A%';

59. WAQTD the details of employees whose third character is I.

SELECT * FROM EMP
WHERE ENAME LIKE '__I

60. SELECT * FROM EMP
WHERE ENAME LIKE 'M%N';

61. WAQTD the details of employees who do not receive commission.

SELECT * FROM EMP
WHERE COMM IS NULL;

62. WAQTD the names of employees who do not receive commission.

SELECT ENAME FROM EMP
WHERE COMM IS NULL;

63. WAQTD the details of employees whose manager number is NULL.

SELECT * FROM EMP
WHERE MGR IS NULL;

64. WAQTD the details of employees who receive commission.

SELECT * FROM EMP
WHERE COMM IS NOT NULL;

65. WAQTD the names and commission of employees who receive commission.

SELECT ENAME, COMM FROM EMP
WHERE COMM IS NOT NULL;

66. WAQTD the details of employees who have a manager.

SELECT * FROM EMP
WHERE MGR IS NOT NULL;

67.WAQTD the details of employees whose annual salary is greater than 30000.

SELECT * FROM EMP
WHERE SAL * 12 > 30000;

68. WAQTD the name and salary of employees whose annual salary is less than 20000.

SELECT ENAME, SAL FROM EMP
WHERE SAL * 12 < 20000;

69. WAQTD the details of employees whose salary after adding 500 is greater than 2500.

SELECT * FROM EMP
WHERE SAL + 500 > 2500;

70. WAQTD the details of employees whose salary after a 10% increment is greater than 3000.

SELECT * FROM EMP
WHERE SAL + (SAL * 10 / 100) > 3000;

71. WAQTD the details of employees who are working in department 20 and have a salary between 2000 and 4000.

SELECT * FROM EMP
WHERE DEPTNO = 20
AND SAL BETWEEN 2000 AND 4000;

72. WAQTD the name and salary of employees who are working in department 10 or 20 and earning more than 2000.

SELECT ENAME, SAL FROM EMP
WHERE DEPTNO IN (10, 20)
AND SAL > 2000;

73. WAQTD the details of employees whose job is CLERK or ANALYST and whose salary is greater than 1000.

SELECT * FROM EMP
WHERE JOB IN ('CLERK', 'ANALYST')
AND SAL > 1000;

74. WAQTD the details of employees working in department 20 whose name starts with S.

SELECT * FROM EMP
WHERE DEPTNO = 20
AND ENAME LIKE 'S%';

75. WAQTD the details of employees whose salary is between 1000 and 3000 and who receive commission.

SELECT * FROM EMP
WHERE SAL BETWEEN 1000 AND 3000
AND COMM IS NOT NULL;

76. WAQTD the details of employees who are working in department 10 or 20 and whose salary is greater than 2000.

SELECT * FROM EMP
WHERE (DEPTNO = 10 OR DEPTNO = 20)
AND SAL > 2000;

77. WAQTD the details of employees who are either CLERK or MANAGER and whose salary is greater than 2000.

SELECT * FROM EMP
WHERE (JOB = 'CLERK' OR JOB = 'MANAGER')
AND SAL > 2000;

78. WAQTD the details of employees who work in department 10 or 30 and earn between 1000 and 3000.

SELECT * FROM EMP
WHERE (DEPTNO = 10 OR DEPTNO = 30)
AND SAL BETWEEN 1000 AND 3000;

79. WAQTD the details of employees who are CLERK or SALESMAN and are not working in department 10.

SELECT * FROM EMP
WHERE JOB IN ('CLERK', 'SALESMAN')
AND DEPTNO <> 10;

80. WAQTD the name and salary of employees whose salary is greater than 2000 but less than 4000.

SELECT ENAME, SAL FROM EMP
WHERE SAL > 2000
AND SAL < 4000;

81. WAQTD the details of employees whose name starts with A or S.

SELECT * FROM EMP
WHERE ENAME LIKE 'A%'
OR ENAME LIKE 'S%';

82. WAQTD the details of employees whose name contains A and who are working in department 30.

SELECT * FROM EMP
WHERE ENAME LIKE '%A%'
AND DEPTNO = 30;

83. WAQTD the details of employees whose job is not CLERK and whose salary is greater than 1500.

SELECT * FROM EMP
WHERE JOB <> 'CLERK'
AND SAL > 1500;

84. WAQTD the name, job and salary of employees who are either MANAGER or ANALYST and earn more than 2500.

SELECT ENAME, JOB, SAL FROM EMP
WHERE JOB IN ('MANAGER', 'ANALYST')
AND SAL > 2500;

85.WAQTD the details of employees who are working in departments other than 10 and 30.

SELECT * FROM EMP
WHERE DEPTNO NOT IN (10, 30);

86. WAQTD the details of employees whose salary is not between 1000 and 3000 and whose job is not CLERK.

SELECT * FROM EMP
WHERE SAL NOT BETWEEN 1000 AND 3000
AND JOB <> 'CLERK';

87. WAQTD the name and salary of employees who have a salary greater than 2000 or receive commission.

SELECT ENAME, SAL FROM EMP
WHERE SAL > 2000
OR COMM IS NOT NULL;

88. WAQTD the total number of employees from the EMP table.

SELECT COUNT(*)
FROM EMP;

89. WAQTD the total number of employee numbers from the EMP table.

SELECT COUNT(EMPNO)
FROM EMP;

90. WAQTD the total number of employees who are receiving commission.

SELECT COUNT(COMM)
FROM EMP;

91. WAQTD the total number of employees who have a manager.

SELECT COUNT(MGR)
FROM EMP;

92. WAQTD the number of employees whose salary is available.

SELECT COUNT(SAL)
FROM EMP;

93. WAQTD the number of different jobs available in the EMP table.

SELECT COUNT(DISTINCT JOB)
FROM EMP;

94. WAQTD the total salary paid to all employees.

SELECT SUM(SAL)
FROM EMP;

95. WAQTD the total commission paid to all employees.

SELECT SUM(COMM)
FROM EMP;

96. WAQTD the total annual salary paid to all employees.

SELECT SUM(SAL) * 12
FROM EMP;

97. WAQTD the total salary of employees working in department 20.

SELECT SUM(SAL) FROM EMP
WHERE DEPTNO = 20;

98. WAQTD the total salary of employees working as MANAGER.

SELECT SUM(SAL) FROM EMP
WHERE JOB = 'MANAGER';

99. WAQTD the total salary paid to employees in each department.

SELECT DEPTNO, SUM(SAL)
FROM EMP
GROUP BY DEPTNO;

100. WAQTD the number of employees working in each department.

SELECT DEPTNO, COUNT(*)
FROM EMP
GROUP BY DEPTNO;

101. WAQTD the average salary of employees in each department.

SELECT DEPTNO, AVG(SAL)
FROM EMP
GROUP BY DEPTNO;

102. WAQTD the highest salary in each department.

SELECT DEPTNO, MAX(SAL)
FROM EMP
GROUP BY DEPTNO;

103. WAQTD the total salary paid for each job.

SELECT JOB, SUM(SAL)
FROM EMP
GROUP BY JOB;

104. WAQTD the number of employees working in each job.

SELECT JOB, COUNT(*)
FROM EMP
GROUP BY JOB;

105. WAQTD the average salary for each job.

SELECT JOB, AVG(SAL)
FROM EMP
GROUP BY JOB;

106. WAQTD the highest salary for each job.

SELECT JOB, MAX(SAL)
FROM EMP
GROUP BY JOB;

107. WAQTD the department number, total salary, average salary, highest salary and lowest salary for each department.

SELECT DEPTNO,SUM(SAL),AVG(SAL),MAX(SAL),MIN(SAL)
FROM EMP
GROUP BY DEPTNO;

108. WAQTD the job, number of employees, total salary and average salary for each job.

SELECT JOB,COUNT(*),SUM(SAL),AVG(SAL)
FROM EMP
GROUP BY JOB;

109. WAQTD the department number, number of employees and total salary for each department.

SELECT DEPTNO,COUNT(*),SUM(SAL)
FROM EMP
GROUP BY DEPTNO;

110. WAQTD the department number and average salary with the alias AVERAGE_SALARY.

SELECT DEPTNO,AVG(SAL) AS AVERAGE_SALARY
FROM EMP
GROUP BY DEPTNO;

111. WAQTD the job and number of employees with suitable aliases.

SELECT JOB,COUNT(*) AS TOTAL_EMPLOYEES
FROM EMP
GROUP BY JOB;

112. WAQTD the department number, highest salary and lowest salary with suitable aliases.

SELECT DEPTNO,MAX(SAL) AS HIGHEST_SALARY,MIN(SAL) AS LOWEST_SALARY
FROM EMP
GROUP BY DEPTNO;

113. WAQTD the number of employees in each department.

SELECT DEPTNO, COUNT(*) FROM EMP
GROUP BY DEPTNO;

114. WAQTD the number of employees receiving commission in each department.

SELECT DEPTNO, COUNT(COMM)
FROM EMP
GROUP BY DEPTNO;

115. WAQTD the number of employees having a manager in each department.

SELECT DEPTNO, COUNT(MGR)
FROM EMP
GROUP BY DEPTNO;

116. WAQTD the number of different jobs in each department.

SELECT DEPTNO, COUNT(DISTINCT JOB)
FROM EMP
GROUP BY DEPTNO;

117. WAQTD the number of different salaries in each department.

SELECT DEPTNO, COUNT(DISTINCT SAL)
FROM EMP
GROUP BY DEPTNO;

118. WAQTD the total salary paid in each department.

SELECT DEPTNO, SUM(SAL) FROM EMP
GROUP BY DEPTNO;

119. WAQTD the total commission received by employees in each department.

SELECT DEPTNO, SUM(COMM) FROM EMP
GROUP BY DEPTNO;

120. WAQTD the total annual salary paid in each department.

SELECT DEPTNO, SUM(SAL * 12)
FROM EMP
GROUP BY DEPTNO;

121. WAQTD the total salary of employees in each job.

SELECT JOB, SUM(SAL)
FROM EMP
GROUP BY JOB;

122. WAQTD the average salary in each department.

SELECT DEPTNO, AVG(SAL) FROM EMP
GROUP BY DEPTNO;

123. WAQTD the average commission in each department.

SELECT DEPTNO, AVG(COMM)
FROM EMP
GROUP BY DEPTNO;

124.WAQTD the average annual salary in each department.

SELECT DEPTNO, AVG(SAL * 12)
FROM EMP
GROUP BY DEPTNO;

125.WAQTD the average salary for each job.

SELECT JOB, AVG(SAL)
FROM EMP
GROUP BY JOB;

126. WAQTD the total salary in each department for employees earning more than 1500.

SELECT DEPTNO, SUM(SAL)
FROM EMP
WHERE SAL > 1500
GROUP BY DEPTNO;

127. WAQTD the average salary in each department for employees earning more than 2000.

SELECT DEPTNO, AVG(SAL)
FROM EMP
WHERE SAL > 2000
GROUP BY DEPTNO;

128. WAQTD the number of employees in each department whose salary is greater than 1000.

SELECT DEPTNO, COUNT(*)
FROM EMP
WHERE SAL > 1000
GROUP BY DEPTNO;

129. WAQTD the highest salary in each department for employees earning more than 2000.

SELECT DEPTNO, MAX(SAL)
FROM EMP
WHERE SAL > 2000
GROUP BY DEPTNO;

130. WAQTD the lowest salary in each department for employees earning more than 1000

SELECT DEPTNO, MIN(SAL)
FROM EMP
WHERE SAL > 1000
GROUP BY DEPTNO;

131. WAQTD the total salary in each department for departments 10, 20 and 30.

SELECT DEPTNO, SUM(SAL)
FROM EMP
WHERE DEPTNO IN (10, 20, 30)
GROUP BY DEPTNO;

132. WAQTD the average salary for each job where the job is CLERK, MANAGER or SALESMAN.

SELECT JOB, AVG(SAL)
FROM EMP
WHERE JOB IN ('CLERK', 'MANAGER', 'SALESMAN')
GROUP BY JOB;

134. WAQTD the number of employees in departments 20 and 30 for each job.

SELECT JOB, COUNT(*)
FROM EMP
WHERE DEPTNO IN (20, 30)
GROUP BY JOB;

135. WAQTD the total salary in each department for employees whose salary is between 1000 and 3000.

SELECT DEPTNO, SUM(SAL)
FROM EMP
WHERE SAL BETWEEN 1000 AND 3000
GROUP BY DEPTNO;

136. WAQTD the average salary in each department for employees whose salary is between 1500 and 3000.

SELECT DEPTNO, AVG(SAL)
FROM EMP
WHERE SAL BETWEEN 1500 AND 3000
GROUP BY DEPTNO;

137. WAQTD the number of employees in each department whose salary is between 1000 and 2500.

SELECT DEPTNO, COUNT(*)
FROM EMP
WHERE SAL BETWEEN 1000 AND 2500
GROUP BY DEPTNO;

138. WAQTD the number of employees in each department whose name starts with S.

SELECT DEPTNO, COUNT(*)
FROM EMP
WHERE ENAME LIKE 'S%'
GROUP BY DEPTNO;

139. WAQTD the total salary in each department for employees whose name contains A.

SELECT DEPTNO, SUM(SAL)
FROM EMP
WHERE ENAME LIKE '%A%'
GROUP BY DEPTNO;

140. WAQTD the average salary for each job among employees whose name ends with N.

SELECT JOB, AVG(SAL)
FROM EMP
WHERE ENAME LIKE '%N'
GROUP BY JOB;

141. WAQTD the number of employees receiving commission and not receiving commission in each department.

SELECT DEPTNO, COUNT(*)
FROM EMP
WHERE COMM IS NULL
GROUP BY DEPTNO;

142. WAQTD the total commission received in each department by employees who receive commission.

SELECT DEPTNO, SUM(COMM)
FROM EMP
WHERE COMM IS NOT NULL
GROUP BY DEPTNO;

143. WAQTD the average commission in each department for employees receiving commission.

SELECT DEPTNO, AVG(COMM)
FROM EMP
WHERE COMM IS NOT NULL
GROUP BY DEPTNO;

144. WAQTD the department number and total salary of each department, arranged by total salary in ascending order.

SELECT DEPTNO, SUM(SAL) AS TOTAL_SALARY
FROM EMP
GROUP BY DEPTNO
ORDER BY TOTAL_SALARY ASC;

145. WAQTD the department number and total salary of each department, arranged by total salary in descending order.

SELECT DEPTNO, SUM(SAL) AS TOTAL_SALARY
FROM EMP
GROUP BY DEPTNO
ORDER BY TOTAL_SALARY DESC;

146. WAQTD the job and average salary for each job, arranged by average salary in descending order.

SELECT JOB, AVG(SAL) AS AVERAGE_SALARY
FROM EMP
GROUP BY JOB
ORDER BY AVERAGE_SALARY DESC;

147. WAQTD the department number and number of employees in each department, arranged by number of employees in descending order.

SELECT DEPTNO, COUNT(*) AS TOTAL_EMPLOYEES
FROM EMP
GROUP BY DEPTNO
ORDER BY TOTAL_EMPLOYEES DESC;

148. WAQTD the department number having more than one employee.

SELECT DEPTNO
FROM EMP
GROUP BY DEPTNO
HAVING COUNT(*) > 1;

149. WAQTD the departments where the total salary is greater than 9000.

SELECT DEPTNO, SUM(SAL) AS TOTAL_SALARY
FROM EMP
GROUP BY DEPTNO
HAVING SUM(SAL) > 9000;

150. WAQTD the departments where the average salary is greater than 2000.

SELECT DEPTNO, AVG(SAL) AS AVERAGE_SALARY
FROM EMP
GROUP BY DEPTNO
HAVING AVG(SAL) > 2000;

151. WAQTD the jobs having more than two employees.

SELECT JOB, COUNT(*) AS TOTAL_EMPLOYEES
FROM EMP
GROUP BY JOB
HAVING COUNT(*) > 2;

152. WAQTD the jobs where the total salary is greater than 5000.

SELECT JOB, SUM(SAL) AS TOTAL_SALARY
FROM EMP
GROUP BY JOB
HAVING SUM(SAL) > 5000;

153. WAQTD the departments where the highest salary is greater than 3000.

SELECT DEPTNO, MAX(SAL) AS HIGHEST_SALARY
FROM EMP
GROUP BY DEPTNO
HAVING MAX(SAL) > 3000;

154. WAQTD the departments where the lowest salary is greater than 1000.

SELECT DEPTNO, MIN(SAL) AS LOWEST_SALARY
FROM EMP
GROUP BY DEPTNO
HAVING MIN(SAL) > 1000;































